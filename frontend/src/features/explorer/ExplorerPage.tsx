export function ExplorerPage() {
  return (
    <section className="page-section">
      <p className="eyebrow">Data Explorer</p>
      <h2>Inspect source records without browser-side financial totals</h2>
      <p>
        Transactions and payouts will use server-side filters, cursor pagination, and receipt-linked exports.
      </p>
      <div className="empty-card">Explorer API integration is queued after the deterministic query engine.</div>
    </section>
  );
}

# Frontend

Barebones frontend workspace.

Use this folder for the frontend application only. Keep finance calculations in the backend; the frontend should render answers, confidence, and masked source evidence returned by the API.

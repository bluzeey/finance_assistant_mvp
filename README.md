# Finance Assistant MVP

Barebones project shell retained for reference.

## Structure

```text
backend/    Backend work area
frontend/   Frontend work area
```

Docker, CI/templates, generated artifacts, docs/spec bundles, contracts, evaluation packs, scripts/tests, local env files, and backlog files were removed to keep this folder minimal.

The canonical active app is expected to be `../finance-assistant`.
